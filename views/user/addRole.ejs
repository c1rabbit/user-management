<%- partial('submenu.ejs') %>
<div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3">
  <h2>Add New Role</h2>

    <%- partial('../error.ejs') %>

    <form method="post" action="addRole" >
      <input type="text" name="role" placeholder="Role Name" class="form-control"
        value="<%= (typeof message != 'undefined') ? req.param('role') : '' %>"  required />
      <button class="btn btn-default pull-right" type="submit" >Add Role</button>
    </form>

</div>
